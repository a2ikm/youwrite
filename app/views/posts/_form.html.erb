<% form_for :post do |f| %>
  <table>
    <tr>
      <th>Name</th>
      <td><%= f.text_field :name, :class => "xlarge" %></td>
    </tr>
    <tr>
      <th>Body</th>
      <td><%= f.text_area :body %></td>
    </tr>
  </table>
  <p><%= f.submit %></p>
<% end %>